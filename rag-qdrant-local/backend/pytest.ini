[pytest]
asyncio_mode = auto
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    eval: runs the retrieval quality scorecard against a live backend (manual; deselect with -m "not eval")
